{
  "name": "shared-projects-management-sp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node -e \"require('fs').mkdirSync('dist',{recursive:true}); require('fs').cpSync('outputs','dist',{recursive:true});\""
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "google-auth-library": "^10.9.0"
  }
}
